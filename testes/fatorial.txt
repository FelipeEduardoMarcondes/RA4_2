# MODO: INTEIRO

# Calcula fatorial de 1 até 8
# Fórmula: FAT = 1 * 2 * 3 * ... * 8

# Inicialização
(1 N)
(1 FAT)

# Loop: WHILE (N <= 8) { FAT = FAT * N; N = N + 1; }
# Em RPN: (condição corpo while)
# Corpo retorna o último valor (N incrementado)
(
    (N 8 <=)               # Condição: N <= 8
    (
        ((FAT N *) FAT)    # FAT = FAT * N
        ((N 1 +) N)        # N = N + 1
        +                  # Combina (retorna soma, mas não importa)
    )
    while
)

# Resultado final
(FAT)